#pragma once
#ifndef TRANSFORM_HPP
#define TRANSFORM_HPP

#include "raymath.h"

namespace render {

using Vector3 = Vector3;            // Render Vectors are raylib vectors
using Quaternion = Quaternion;      // Render Quaternions are raylib quaternions
using Transform = Transform;        // Render Transforms are raylib transforms

}


#endif // TRANSFORM_HPP